name: Daily Financial Data Scrape (DEPRECATED)

# ──────────────────────────────────────────────────────────────────────────────
# ⚠️ DEPRECATED — This workflow has been replaced by fetch-and-deploy.yml
#
# The Python-based scrapers (SCRAPINGANT) have been superseded by the Node.js
# fetch-real-data.cjs pipeline which uses Serper.dev + Tavily APIs directly.
#
# The new pipeline runs every 6 hours and includes:
#   - Real news fetching via Serper.dev
#   - Article content generation via Tavily
#   - Market data (indices, crypto, fear/greed, yield curve)
#   - Automatic build + deploy to Cloudflare Pages
#
# This file is kept for reference only. Do not enable.
# ──────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:  # Disabled schedule — only manual trigger for emergencies

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: "\u26A0\uFE0F Deprecated workflow"
        run: |
          echo "⚠️ This workflow is DEPRECATED. Use 'Fetch Real Data & Deploy' instead."
          echo "Run: gh workflow run fetch-and-deploy.yml"
          exit 1
