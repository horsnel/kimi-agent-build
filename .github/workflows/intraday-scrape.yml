name: Intraday Market Data Scrape (DEPRECATED)

# ──────────────────────────────────────────────────────────────────────────────
# ⚠️ DEPRECATED — This workflow has been replaced by fetch-and-deploy.yml
#
# The Python-based intraday scrapers (SCRAPINGANT) have been superseded by the
# Node.js fetch-real-data.cjs pipeline which uses Serper.dev + Tavily APIs.
#
# The new pipeline runs every 6 hours and handles all data fetching + deployment.
# For real-time intraday data, consider using the workflow_dispatch trigger on
# fetch-and-deploy.yml with fetch_type=market.
#
# This file is kept for reference only. Do not enable.
# ──────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:  # Disabled schedule — only manual trigger for emergencies

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: "\u26A0\uFE0F Deprecated workflow"
        run: |
          echo "⚠️ This workflow is DEPRECATED. Use 'Fetch Real Data & Deploy' instead."
          echo "Run: gh workflow run fetch-and-deploy.yml -f fetch_type=market"
          exit 1
